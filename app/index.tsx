import { SafeAreaViewCustom, SplashScreenLoading } from "@/components";
import "@/globals.css";

const index = () => {
  return (
    <SafeAreaViewCustom>
      <SplashScreenLoading />
    </SafeAreaViewCustom>
  );
};

export default index;
