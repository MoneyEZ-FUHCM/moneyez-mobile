import { SafeAreaViewCustom, SplashScreen } from "@/components";
import "@/globals.css";
import React from "react";

const index = () => {
  return (
    <SafeAreaViewCustom>
      <SplashScreen />
    </SafeAreaViewCustom>
  );
};

export default index;
